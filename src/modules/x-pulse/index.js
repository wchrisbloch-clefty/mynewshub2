// X Pulse — public interface. Client component lives here; the serverless half is
// api/x-pulse.js (platform requires it under api/). Together they need only a topic
// string + XAI_API_KEY.
export { XPulse } from './XPulse.jsx';
